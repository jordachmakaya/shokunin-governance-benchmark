# Gemini routing

The canonical repository instructions are in `AGENTS.md`.

Load `MEMORY.md` and `.shokunin/systems/ACTIVE_ASSIGNMENTS.json` before acting. Gemini is currently the Integration Owner and owns BoxedZone ZB2 (`packages/benchmark-kit/**`). ZB1 (`packages/core/**`) is sealed and verified.
